import React from 'react';
import ReactDOM from 'react-dom';
import InitializedBootstrap from './init/InitializedBootstrap';

ReactDOM.render(
  <InitializedBootstrap />,
  document.getElementById('root')
);